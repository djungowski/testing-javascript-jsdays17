class Calculator {
  sum(a, b) {
    return a + b;
  }

  subtract() {

  }

  multiply() {

  }

  multiplyWithRandom(number, math) {
    return number * math.random();
  }

  multiplyWithMathRandom(number) {
    return number * Math.random();
  }
}